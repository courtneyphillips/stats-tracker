<h1>{{name}}</h1>

<div class="row">
  <div class="col-md-6">
    <div class="well well-lg">
      <h3>Players</h3>

      {{#each player}}
        <li>{{#link-to 'player' this.id}}{{playerName}}{{/link-to}}</li>
      {{/each}}
    </div>
  </div>

  <div class="col-md-6">
    <div class="well well-lg">
      <h1>{{#link-to 'newPlayer' this.id}}<span class="btn btn-primary btn-block">Add a Player</span>{{/link-to}}</h1>
      {{outlet}}
    </div>
  </div>
</div>
